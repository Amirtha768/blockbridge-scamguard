# URL of the deployed backend API server.
# For local development this is unused — the Vite proxy handles /api/* calls.
# For production (Vercel) set this in the Vercel dashboard under:
#   Settings > Environment Variables > Add variable
VITE_API_URL=https://blockbridge-scamguard.onrender.com
